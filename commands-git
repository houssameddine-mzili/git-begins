Pour cloner (copier) un référentiel Git depuis un serveur distant
(comme GitHub) vers le  système local:
git clone https://github.com/votre_nom_utilisateur/nom_du_referentiel.git

les 3 commandes pour "push":

Push sur la branche principale (généralement "main" ou "master") :
git push origin main

Push de toutes les branches locales :
git push --all origin

Push de tags (étiquettes) :
git push --tags origin

la commande pour faire un pull : 
git pull origin nom_de_la_branche
example : git pull origin main

commande pour créer une branche :
$ git checkout -b <branch-name>
switche :
$ git checkout <existing_branch>
